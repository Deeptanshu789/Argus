# datasets & weights — never in git
datasets/
runs/
*.pt
*.onnx
*_openvino_model/
ml/train.log

# python
.venv/
__pycache__/
*.py[cod]

# node
node_modules/
dist/

# env
.env

# next / node
.next/
next-env.d.ts
.env.local
tsconfig.tsbuildinfo
ml/demo_out/

# Synthetic test clips from ml/make_demo_clips.py — regenerate, never commit.
demo/*.mp4

# Local working notes — machine state, not project rules.
.claude/CONTEXT.md

# JVM crash dumps (an OpenVINO dependency leaves these behind)
hs_err_pid*.log

# Video uploaded through the dashboard. Operator files, never committed.
uploads/

# Self-signed certificate for the HTTPS dev proxy. Never committed.
.certs/
